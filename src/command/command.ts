export enum Command {
  // generic command that brings up a list of actions to run on the current file
  RunAction = "legacy-csproj.run-action",
  AddFile = "legacy-csproj.add-file",
  RemoveFile = "legacy-csproj.remove-file",
  SyncFiles = "legacy-csproj.sync-files",
}
